"use client";
export default function DashboardError({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><section className="max-w-md rounded-2xl border border-slate-200 bg-white p-7 text-center shadow-sm"><h1 className="text-xl font-bold text-slate-900">We couldn’t load the dashboard</h1><p className="mt-2 text-sm text-slate-500">Check your connection and try again. Your project data has not been changed.</p><button onClick={reset} className="mt-5 rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-slate-950">Try again</button></section></main>; }
