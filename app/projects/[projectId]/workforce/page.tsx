import ProjectModuleEmpty from "@/components/project/ProjectModuleEmpty"; import { getProject } from "@/lib/services/project-data"; export default async function Page({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <ProjectModuleEmpty title="Workforce" description="Project workforce assignments, rates, and labour cost." project={await getProject(projectId)} />; }
