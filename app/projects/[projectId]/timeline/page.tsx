import ProjectModuleEmpty from "@/components/project/ProjectModuleEmpty"; import { getProject } from "@/lib/services/project-data"; export default async function Page({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <ProjectModuleEmpty title="Timeline" description="Project dates, task deadlines, milestones, and delays." project={await getProject(projectId)} />; }
